// const schema = require('../../middlewares/schemas');
// const { HTTP_UNPROCESSABLE_ENTITY } = require('../../utils/errorsMap');

// const salesValidation = (sales) => {
//   sales.forEach((id) => {
//     const { error } = schema.salesValidation.validate(id.productId);
//     console.log(id)
//   });

//   return { type: null, message: 'HTTP_STATUS_OK' };
// };

// module.exports = { salesValidation };
